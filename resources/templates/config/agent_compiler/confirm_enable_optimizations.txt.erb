<banner>Compile the agent with optimizations?</banner>

Compiling the agent with optimizations will make it faster, but it will take
longer to compile and it requires <b>at least 2 GB of memory</b>.


<red>WARNING:</red> <yellow>Apache doesn't seem to be compiled with the 'prefork' MPM</yellow>

Passenger has only been tested on Apache with the 'prefork' MPM. Your Apache
installation is compiled with the '<%= @current_mpm %>' MPM. We recommend you
to abort this installer and to recompile Apache with the 'prefork' MPM.

<b>Press Ctrl-C to abort this installer (recommended).</b>
<b>Press Enter if you want to continue with installation anyway.</b>

<banner>Nginx core <%= @version %> isn't installed</banner>

Phusion Passenger Standalone will automatically install it into:

  <b><%= @dir %></b>

This will only be done once. Please sit back and relax while installation is
in progress.

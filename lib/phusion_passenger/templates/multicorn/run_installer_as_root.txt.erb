<red>Permission problems</red>

Multicorn must be able to write to the following directory:

  <b><%= @dir %></b>

But it can't do that, because you're running Multicorn as <b><%= `whoami`.strip %></b>.
Please re-run Multicorn as <b>root</b>.